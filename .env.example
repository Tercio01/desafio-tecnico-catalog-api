# 🚀 Server Configuration
PORT=3000
NODE_ENV=development

# 📊 Database - MongoDB Atlas
# Format: mongodb+srv://<username>:<password>@cluster.mongodb.net/database-name
# 
# SETUP INSTRUCTIONS:
# 1. Go to: https://cloud.mongodb.com
# 2. Database Access → Create new user
# 3. Copy connection string
# 4. Replace <username> and <password> with actual credentials
# 5. Replace cluster name if different
#
# IMPORTANT: 
# - Use real credentials here (for local development only)
# - NEVER commit this file with real credentials
# - For production, use GitHub Secrets or managed secret stores
#
# LOCAL DEVELOPMENT:
MONGODB_URI=mongodb://localhost:27017/catalog
# 
# OR MONGODB ATLAS (uncomment and update with real credentials):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/catalog_db

# 🔴 Redis Cache
# Use localhost:6379 for local development
# Or connect to AWS ElastiCache in production
REDIS_URL=redis://localhost:6379

# 🔐 JWT Authentication
# Generate a secure secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Minimum 32 characters recommended
# 
# SECURITY NOTES:
# - Use strong random string (min 32 chars)
# - Never use same secret in different environments
# - Rotate secrets every 90 days
# - Store in environment variables only (not in code)
JWT_SECRET=your_generated_secret_here_minimum_32_chars
JWT_EXPIRES_IN=24h

# 📚 Swagger/OpenAPI Documentation
SWAGGER_ENABLED=true
SWAGGER_PATH=/api-docs
OPENAPI_PATH=/openapi.json

# 🔒 CORS Configuration
CORS_ORIGIN=http://localhost:3000

# 📈 Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# 📚 SETUP GUIDE:
# 
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update credentials:
#    - For local: use MongoDB local or Docker
#    - For Atlas: get credentials from cloud.mongodb.com
#
# 3. Generate JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 4. Install and run:
#    npm install
#    npm run dev
#
# 5. Access API:
#    http://localhost:3000/api-docs
#
# 🔐 SECURITY REMINDERS:
# - .env is in .gitignore (never committed)
# - .env.example shows structure without secrets
# - Rotate MongoDB credentials every 90 days
# - See docs/SECURITY.md for detailed guidelines
# - See .github/SECURITY_INCIDENT_RESPONSE.md for incident procedures
